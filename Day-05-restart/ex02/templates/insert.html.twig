<h1>Ajouter un utilisateur</h1>

{{ form_start(form) }}
    {{ form_widget(form) }}
{{ form_end(form) }}

{% if message is defined %}
    <p><strong>{{ message }}</strong></p>
{% endif %}

<a href="{{ path('ex02select') }}">🔍 Voir les utilisateurs ajoutés</a>
<form method="get" action="{{ path('ex02delete') }}">
    <button type="submit">Supprimer la table</button>
</form>